---
phase: 32-perp-analytics-and-cli-report
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/cli/report.ts
autonomous: false

must_haves:
  truths:
    - "npm run report -- --type perp prints a perp-specific analytics section"
    - "The perp section shows win rate by direction (long / short separately)"
    - "The perp section shows average leverage, net funding cost, and net perp P&L"
    - "Spot and perp P&L are printed in separate sections with no blended total"
    - "When perp_trades is empty, the command prints 'No perp trades found' and exits cleanly"
  artifacts:
    - path: "src/cli/report.ts"
      provides: "--type perp branch in Commander action"
      contains: "sessionType === 'perp'"
  key_links:
    - from: "src/cli/report.ts"
      to: "src/perp/perp-state-store.ts listClosedTrades()"
      via: "perpStateStore.listClosedTrades()"
      pattern: "listClosedTrades"
    - from: "src/cli/report.ts perp analytics"
      to: "src/core/decimal.ts d()"
      via: "d() for all aggregate math"
      pattern: "d\\("
---

<objective>
Extend `npm run report` with a `--type perp` branch that reads all closed perp trades from
PerpStateStore and prints directional win rates, average leverage, net funding cost, and net P&L
in a dedicated section — completely separate from spot analytics.

Purpose: ANALYTICS-02 and ANALYTICS-03 compliance. The user can now evaluate perp trading
performance from the CLI using the same workflow as spot report.
Output: Modified src/cli/report.ts with a perp branch; no new files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/report.ts
@src/cli/shared/output.ts
@src/perp/perp-state-store.ts
@.planning/phases/32-perp-analytics-and-cli-report/32-perp-analytics-and-cli-report-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --type perp branch to report.ts</name>
  <files>src/cli/report.ts</files>
  <action>
    Extend the existing Commander action in src/cli/report.ts with a perp branch. The existing
    option declaration is:
      .option('--type <type>', 'Session type: backtest|paper|live', 'backtest')
    Update the description string to include 'perp':
      .option('--type <type>', 'Session type: backtest|paper|live|perp', 'backtest')

    Add a new else-if branch after the existing 'live' branch (before the final else):

    } else if (sessionType === 'perp') {
      // Import at top of file: import { PerpStateStore } from '../perp/perp-state-store.js';
      // Import at top of file: import { d, ZERO } from '../core/decimal.js';
      const perpStore = new PerpStateStore();
      try {
        const trades = perpStore.listClosedTrades();

        if (trades.length === 0) {
          out.warn('No perp trades found.');
          return;
        }

        // Directional split
        const longTrades = trades.filter((t) => t.direction === 'long');
        const shortTrades = trades.filter((t) => t.direction === 'short');

        // Long win rate
        const longWins = longTrades.filter((t) => d(t.realizedPnl).gt(ZERO));
        const longWinRate = longTrades.length > 0
          ? d(longWins.length).div(d(longTrades.length))
          : ZERO;

        // Short win rate
        const shortWins = shortTrades.filter((t) => d(t.realizedPnl).gt(ZERO));
        const shortWinRate = shortTrades.length > 0
          ? d(shortWins.length).div(d(shortTrades.length))
          : ZERO;

        // Average leverage
        const avgLeverage = trades
          .reduce((sum, t) => sum.plus(d(String(t.leverage))), ZERO)
          .div(d(trades.length));

        // Net funding cost (signed: negative = paid, positive = received)
        const netFundingCost = trades.reduce(
          (sum, t) => sum.plus(d(t.cumulativeFundingCost)), ZERO
        );

        // Net perp P&L
        const netPerpPnl = trades.reduce((sum, t) => sum.plus(d(t.realizedPnl)), ZERO);

        out.banner('Perp Trading Report');
        out.table('Trade Count',      String(trades.length));
        out.table('Long Trades',      `${longTrades.length}  (win rate: ${longWinRate.mul(100).toFixed(1)}%)`);
        out.table('Short Trades',     `${shortTrades.length}  (win rate: ${shortWinRate.mul(100).toFixed(1)}%)`);
        out.table('Avg Leverage',     `${avgLeverage.toFixed(1)}x`);
        out.table('Net Funding Cost', netFundingCost.toFixed(8));
        out.table('Net Perp P&L',     netPerpPnl.toFixed(8));

        out.success('Report complete');
      } finally {
        perpStore.close();
      }
      return;   // early return — skip the spot computePerformanceReport() call below

    Note: the existing code after the branch chain calls computePerformanceReport() and prints
    spot metrics. The early return ensures the perp branch NEVER reaches that code, satisfying
    ANALYTICS-03 (no blended summary).

    Required top-of-file imports to add (only if not already present):
      import { PerpStateStore } from '../perp/perp-state-store.js';
      import { d, ZERO } from '../core/decimal.js';

    Do NOT remove or modify any existing spot/backtest/paper/live branches.
    Do NOT introduce a try/catch around the full action — the existing outer try/catch handles
    errors for all branches including perp; perpStore.close() belongs in a finally inside the
    perp branch only.
  </action>
  <verify>
    npx tsx src/cli/report.ts --type perp
    Expected output when DB has no trades: warns "No perp trades found." and exits 0.

    To smoke-test with data, run the paper perp engine briefly or insert a row manually:
      npx tsx -e "
        import { PerpStateStore } from './src/perp/perp-state-store.js';
        const s = new PerpStateStore();
        s.recordTrade({ sessionId:'test-1', instrument:'BIP-20DEC30-CDE', direction:'long',
          leverage:5, entryPrice:'95000', exitPrice:'96000', size:'0.01',
          cumulativeFundingCost:'-0.00050000', realizedPnl:'10.00000000',
          openedAt: Date.now()-60000, closedAt: Date.now(), closeReason:'manual' });
        s.close();
      "
    Then: npx tsx src/cli/report.ts --type perp
    Expected: banner "Perp Trading Report", Trade Count 1, Long Trades 1 (win rate: 100.0%),
    Short Trades 0 (win rate: 0.0%), Avg Leverage 5.0x, Net Funding Cost -0.00050000,
    Net Perp P&L 10.00000000
  </verify>
  <done>
    --type perp prints a perp analytics section with correct directional win rates, average
    leverage, net funding cost, and net P&L. Zero perp trades prints the warning and exits
    cleanly. No spot metrics appear when --type perp is used.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify perp report CLI output</name>
  <action>Present the verification steps below to the user and pause for confirmation.</action>
  <what-built>
    perp report branch in npm run report -- --type perp. Reads all rows from perp_trades via
    PerpStateStore.listClosedTrades(), computes directional win rates / avg leverage / net funding
    / net P&L using d(), prints each in a dedicated out.banner section. Spot sections are
    completely absent from the perp path.
  </what-built>
  <how-to-verify>
    1. Run: npm run report -- --type perp
       If perp_trades is empty: confirm "No perp trades found" appears and process exits 0.

    2. Insert a test record and re-run to confirm analytics output:
       npx tsx -e "
         import { PerpStateStore } from './src/perp/perp-state-store.js';
         const s = new PerpStateStore();
         s.recordTrade({ sessionId:'t1', instrument:'BIP-20DEC30-CDE', direction:'long',
           leverage:5, entryPrice:'95000', exitPrice:'96000', size:'0.01',
           cumulativeFundingCost:'-0.00050000', realizedPnl:'10.00000000',
           openedAt: Date.now()-60000, closedAt: Date.now() });
         s.close();
       "
       npm run report -- --type perp
       Confirm: Trade Count 1, Long Trades with win rate, Avg Leverage 5.0x, net values shown.

    3. Run: npm run report -- --type backtest --latest (or --type paper --latest if sessions exist)
       Confirm: spot report still works correctly (no regression).

    4. Confirm: npm run report -- --type perp output has NO "Win Rate", "Avg Win", "Avg Loss",
       "Profit Factor" fields (those are spot-only metrics).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. npx tsx src/cli/report.ts --type perp — no crash, correct output
2. npm run report -- --type perp — same via npm script
3. npm run report -- --latest (default backtest) — existing path unaffected
4. grep -n "sessionType === 'perp'" src/cli/report.ts — shows branch exists
5. grep -n "listClosedTrades" src/cli/report.ts — shows query call
6. grep -n "ZERO\|d(" src/cli/report.ts — confirms d()/ZERO used for analytics math
7. npx vitest run — full test suite passes (no regressions)
</verification>

<success_criteria>
- npm run report -- --type perp prints a perp section with directional win rates, average
  leverage, net funding cost, and net P&L
- Zero trades case prints a warning and exits cleanly (no crash, exit code 0)
- Spot analytics (computePerformanceReport) is never called from the perp branch
- Existing --type backtest / paper / live branches are unmodified and still pass
- No blended spot+perp summary metric exists anywhere in the output
</success_criteria>

<output>
After completion, create `.planning/phases/32-perp-analytics-and-cli-report/32-perp-analytics-and-cli-report-02-SUMMARY.md`
</output>
